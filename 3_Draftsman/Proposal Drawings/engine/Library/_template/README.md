# <COMPONENT NAME>

Copy this folder, rename it, and fill this file in. **Read `../GOLDEN_RULES.md` first** — most of
the ways this goes wrong are silent.

## What it draws

One line per drawer, with its arguments.

## The numbers, and where each came from

| | value | source |
|---|---|---|
|  |  | traced from `reference/<file>` / stylised |

State plainly which numbers are **traced** and which are **stylised** (rule 20). A stylised shape
under correct dimensions is honest; invented dimensions are not.

## Layers and pens

| element | layer | ACI | pen |
|---|---|---|---|

Layers come from `Rule_Book/PEB_LAYERS.csv` (rule 7). A new one goes in the CSV first (rule 8).
Remember the PDF is monochrome — only **pen** distinguishes anything on the deliverable (rule 4).

## Status

Not started.

## Sync

Not synced. Three edits when it is ready — see the last section of `../GOLDEN_RULES.md`.
